
# Question 1: Generate and plot the time-domain waveform associated to the binary neutron star merger GW170817. How much time does the signal last using a lower frequency of 30 Hz?
``` python
import numpy as np
import scipy
import matplotlib.pyplot as plt
from pycbc.waveform import get_td_waveform

x = 1.46
y = 1.27
hp, hc = figures = get_td_waveform(approximant="IMRPhenomT", mass1 = x , mass2 = y, inclination=0, delta_t=1/2048, f_lower = 30)
plt.figure(figsize=plt.figaspect(.2))
plt.plot(hp.sample_times, hp, label = 'Plus Polarization')
plt.plot(hc.sample_times, hc, label = 'Cross Polarization')
plt.xlim(hp.sample_times[0],2)
plt.xlabel('Timme (s)')
plt.ylabel("Strain")
plt.legend()
plt.grid()
print(f"{hp.sample_times[-1]-hp.sample_times[0]} seconds that a signal with lower frequency of 30hz lasts for")
```

# Question 2: Same question for the binary black hole merger GW150914.
``` python
import numpy as np
import scipy
import matplotlib.pyplot as plt
from pycbc.waveform import get_td_waveform

x = 35.6
y = 30.6

hp, hc = figures = get_td_waveform(approximant="IMRPhenomT", mass1 = x, mass2 = y, inclination=0, delta_t=1/2048, f_lower = 30)
plt.figure(figsize=plt.figaspect(.2))
plt.plot(hp.sample_times, hp, label = 'Plus Polarization')
plt.plot(hc.sample_times, hc, label = 'Cross Polarization')
plt.xlim(hp.sample_times[0],.05) #i am so good for knowing this is an array now...
plt.xlabel('Timme (s)')
plt.ylabel("Strain")
plt.legend()
plt.grid()

print(f"{hp.sample_times[-1]-hp.sample_times[0]} seconds that a signal with lower frequency of 30 hz lasts for")
```
